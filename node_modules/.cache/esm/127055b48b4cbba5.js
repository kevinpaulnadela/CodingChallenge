_8b4‍.x([["writeCsv",()=>writeCsv]]);const csvData = require('./main_app');

       function writeCsv() {
  var fs = require('fs');

  fs.writeFile('./out2.csv', csvData, 'utf8', function(err) {
    if (err) {
      console.log('Some error occured - file either not saved or corrupted file saved.');
    } else {
      console.log('CSV file has been saved!');
    }
  });
 }